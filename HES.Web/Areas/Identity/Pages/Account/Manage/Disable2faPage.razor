@page "/Identity/Account/Manage/Disable2fa"
@attribute [Authorize(Roles = ApplicationRoles.AdminRole)]

<NavigationHeader Tab="NavigationHeader.ActiveTab.TwoFA" />

@if (Initialized)
{
    <div class="card">
        <div class="card-body">
            <h4 class="text-navyblue mb-3">Disable two-factor authentication (2FA)</h4>
            <div class="alert alert-warning" role="alert">
                <p>
                    <span class="glyphicon glyphicon-warning-sign"></span>
                    <strong>This action only disables 2FA.</strong>
                </p>
                <p>
                    Disabling 2FA does not change the keys used in authenticator apps. If you wish to change the key
                    used in an authenticator app you should <a href="/Identity/Account/Manage/ResetAuthenticator">reset your authenticator keys.</a>
                </p>
            </div>
            <div>
                <button class="btn btn-danger" type="button" @onclick="DisableTwoFactorAsync">Disable 2FA</button>
            </div>
        </div>
    </div>
}
else if (LoadFailed)
{
    <LoadingFailed ErrorMessage="@ErrorMessage" />
}
else
{
    <LoadingPage />
}